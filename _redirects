# Cloudflare Pages 兜底规则（构建时读取，不会作为静态文件对外提供）
#
# 作用：/tool/<id>/ 正常情况下命中构建期生成的静态页 tool/<id>/index.html；
# 若该工具还没生成静态页（比如新增工具忘了跑 scripts/gen-static.js），
# 这里回退到首页并由 app.js 按路径渲染，避免用户看到 404。
#
# 只兜底 /tool/ 前缀，其它路径不存在时仍返回真正的 404，
# 不写成 /* 是为了避免把任意乱路径都变成 200 而造成「软 404」。
/tool/*  /index.html  200
